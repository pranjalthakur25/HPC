"""Presentation layer: delivery mechanisms (CLI, future API) for application use cases.

Depends on ``hpc_provenance.application`` (use cases, DTOs) and
``hpc_provenance.config`` (the composition root). Contains no business logic.
"""
